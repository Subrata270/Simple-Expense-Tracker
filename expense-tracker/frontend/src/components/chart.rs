// Visualizes expenses by category (optional).
use yew::prelude::*;

// This is a placeholder. For a real chart, use a Yew-compatible chart library or SVG.
#[function_component(Chart)]
pub fn chart() -> Html {
    html! {
        <div class="my-4">
            <p>{"[Chart visualization goes here]"}</p>
        </div>
    }
}